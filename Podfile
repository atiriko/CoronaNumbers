# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Corona Numbers' do
  # Comment the next line if you don't want to use dynamic frameworks
  #use_frameworks!
  use_modular_headers!
pod "SwiftChart"
pod 'Google-Mobile-Ads-SDK'

  # Pods for Corona Numbers

  target 'Corona NumbersTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Corona NumbersUITests' do
    # Pods for testing
  end

end
